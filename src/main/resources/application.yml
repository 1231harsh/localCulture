spring:
  datasource:
    url: jdbc:mysql://localhost:3306/localstore?useSSL=false&serverTimezone=UTC
    username: root
    password: 1231harsh
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
